# Environment variables - NEVER commit these
.env
.env.local
.env.*.local
.env.production
.env.development
frontend/.env
frontend/.env.local
frontend/.env.production
backend/.env
backend/.env.local
frontend/src/.env*
.dev.vars

# Dependencies
node_modules/
.pnp
.pnp.js

# Build
build/
dist/
out/

# Wrangler local dev state (Miniflare R2/cache simulation) — never real content
.wrangler/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Internal documentation — kept local only, not published to GitHub
docs/
sawo-main/docs/

# One-off local staging area for large binary assets (GLBs etc.) awaiting
# upload to R2 via upload-3d-model.js / upload-hero-asset.js — never commit
# the raw asset itself, only the script.
**/scripts/tmp-3d-assets/
